name: Zip Release

on:
  release:
    types: [published]

jobs:
  release:
    name: SmartWater release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Create Zip
        run: |
          zip -r dabpumps.zip ./custom_components/dabpumps/

      - name: Upload Zip to Release
        uses: softprops/action-gh-release@v2.2.1
        with:
          files: dabpumps.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
